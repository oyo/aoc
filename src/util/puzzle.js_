const _ = require('lodash');

const P = {

    prep: T => _.map(T.split('\n'), L => L.split(',')),

    part_1: T => {
        const p = P.prep(T)
        return input.length
    },

    part_2: T => {
        const p = P.prep(T)
        return p.length
    }

}

exports.puzzle = P