const _ = require('lodash')

const P = {

    prep: T => T.split('\n').map(L => L.split(',')),

    part_1: T => {
        const p = P.prep(T)
        return T.length
    },

    part_2: T => {
        const p = P.prep(T)
        return p.length
    }

}

exports.puzzle = P