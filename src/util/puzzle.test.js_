const { puzzle } = require('./puzzle.js');

it ('solves part_1', () => {
    expect(puzzle.part_1('ABAP')).toEqual(1)
})

it ('solves part_2', () => {
    expect(puzzle.part_2('ABAP')).toEqual('3 unique')
})
